<div class="mb-5">
  <%= render @project %>
</div>

<div class="d-flex justify-content-center mb-5">
  <div class="mx-3">
<%
=begin%>
 check the admin with try method and user& method. Both methods work fine
<%
=end
%>
    <%= link_to "Edit this project", edit_project_path(@project), class:"btn btn-warning" if current_user.try(:admin?) %>
  </div> 
  <div class="mx-3">
      <%= link_to "Back to projects", projects_path, class:"btn btn-secondary" %>
  </div> 
  <div class="mx-3">
      <%= button_to "Destroy this project", @project, method: :delete, class:"btn btn-danger" if current_user&.admin? %>
  </div> 
</div>

