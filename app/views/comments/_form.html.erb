
<%= form_with(model: [post, comment]) do |f| %>
    <div class="form-control">
        <%= f.rich_text_area :body %>
        <%= f.submit "Reply", class:"btn btn-secondary mt-3" %>
    </div>
<% end %>
